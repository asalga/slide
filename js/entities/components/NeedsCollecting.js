export default class NeedsCollecting extends Component {
  constructor(e, cfg) {
    super(e, 'needscollecting');
    let defaults = {

    };
    Utils.applyProps(this, defaults, cfg);
  }

  update(dt) {}
}